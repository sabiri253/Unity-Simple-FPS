using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CrossHair : MonoBehaviour
{
    //Shooting shoot;
    Animator crossAnim;

    private void Start()
    {
       // crossAnim = GetComponent<Animator>();
    }

    // Update is called once per frame
    void Update()
    {
        /*if (shoot.isShooting)
            crossAnim.SetTrigger("Shoot");*/
    }
}